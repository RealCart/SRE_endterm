FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
ENV SERVICE_NAME=profile-service
ENV PORT=8006
EXPOSE 8006
CMD ["sh", "-c", "uvicorn main:app --host 0.0.0.0 --port $PORT"]
